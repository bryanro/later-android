<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppCompatTheme" parent="@style/Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

</resources>
